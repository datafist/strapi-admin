# Railway Environment Variables Template
# Diese Werte in Railway Dashboard setzen!

# Node Environment
NODE_ENV=production

# Database (wird automatisch von Railway gesetzt wenn PostgreSQL hinzugefügt wird)
# DATABASE_URL wird automatisch gesetzt
DATABASE_CLIENT=postgres

# Strapi Secrets (NEUE WERTE GENERIEREN!)
# Generieren mit: node -e "console.log(require('crypto').randomBytes(16).toString('base64'))"

APP_KEYS=CHANGE_ME
API_TOKEN_SALT=CHANGE_ME
ADMIN_JWT_SECRET=CHANGE_ME
TRANSFER_TOKEN_SALT=CHANGE_ME
ENCRYPTION_KEY=CHANGE_ME
JWT_SECRET=CHANGE_ME

# Optional: Host und Port (Railway setzt PORT automatisch)
# HOST=0.0.0.0
# PORT wird von Railway gesetzt
