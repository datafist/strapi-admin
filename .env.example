# ===========================================
# Strapi Environment Configuration
# ===========================================
# Kopiere diese Datei zu .env und passe die Werte an:
#   cp .env.example .env
#
# WICHTIG: .env wird NICHT ins Git committed!
# ===========================================

# -------------------------------------------
# Umgebung
# -------------------------------------------
NODE_ENV=development

# -------------------------------------------
# Datenbank (PostgreSQL)
# -------------------------------------------
DATABASE_CLIENT=postgres
DATABASE_SSL=false

# Für ENTWICKLUNG (Docker auf localhost):
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=strapi_dev
DATABASE_USERNAME=strapi
DATABASE_PASSWORD=strapi_dev_password

# Für PRODUKTION (Docker-Netzwerk):
# DATABASE_HOST=postgres
# DATABASE_NAME=strapi
# DATABASE_PASSWORD=SICHERES_PASSWORT_HIER

# -------------------------------------------
# Server
# -------------------------------------------
HOST=0.0.0.0
PORT=1337

# Für PRODUKTION:
# PUBLIC_URL=https://api.florianbirkenberger.de

# -------------------------------------------
# Secrets
# -------------------------------------------
# Generieren mit: openssl rand -base64 32
#
# ENTWICKLUNG - diese Werte können so bleiben:
APP_KEYS=devkey1,devkey2,devkey3,devkey4
API_TOKEN_SALT=devsalt1234567890
ADMIN_JWT_SECRET=devjwtsecret1234567890
TRANSFER_TOKEN_SALT=devtransfersalt1234567890
ENCRYPTION_KEY=devencryptionkey1234567890
JWT_SECRET=devjwtsecret1234567890

# PRODUKTION - MUSS geändert werden!
# Führe 6x aus: openssl rand -base64 32

# -------------------------------------------
# Seeding (Testdaten)
# -------------------------------------------
# Auf true setzen um bei leeerer DB Testdaten zu laden
SEED_DATA=false
